services:
  backend:
    build: ./backend
    environment:
      CORS_ORIGINS: '["http://localhost:8080"]'
    expose:
      - "8000"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
